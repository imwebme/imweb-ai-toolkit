# imweb skills

이 디렉터리는 imweb 전용 skill 본문을 담는 루트입니다.

현재는 공개 진입점 `SKILL.md`와 도메인별 `references/` 자산이 이 레포로 이관된 상태입니다.

설치된 bundle 안에서는 다음 순서로 보면 됩니다.

- `SKILL.md`
- `docs/`
- `references/`

실제 실행은 별도의 `imweb` CLI 바이너리와 함께 사용합니다.
